Profile API incident — deleted-account handling

Incident context: The profile screen calls a JSON HTTP endpoint and caches successful profiles for the session. Deleted accounts are expected during normal account cleanup. The fixture uses deterministic Response objects and an in-memory cache.

@Noah [high]: examples/profile-api-junior/profile-api-junior.ts treats a 404 payload as a valid profile. Return null for a deleted profile so the UI can show the signed-out state.

@Mika [high]: A 500 response must remain an error and must not populate examples/profile-api-junior/src/profile-cache.ts. Do not convert server outages into empty profiles.

@Lea [medium]: A 200 response missing id or displayName is malformed. Review examples/profile-api-junior/src/profile-client.ts and reject it with a useful error instead of caching partial data.

QA: Cover valid 200 JSON, 404 deletion, 500 failure, malformed success payloads, cache hits, and explicit invalidation. Keep response parsing deterministic and avoid logging profile contents.

Release requirement: Preserve the existing request URL shape and keep the change local to the profile fixture.