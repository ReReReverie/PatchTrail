Collaborative task list incident — sync client

Incident context: The task list receives local button actions and replayed sync events. A stale browser tab can send an action for a task that another device already deleted. Events may arrive out of order, and a reconnect can replay the same sequence number.

@Mia [medium]: Clicking Complete for a task removed by another device crashes the list in examples/task-list-student/task-list-student.ts. Ignore a missing id without mutating the remaining tasks.

@Noah [medium]: Replayed completion events are duplicated by examples/task-list-student/src/task-store.ts. Preserve sequence ordering, but make applying the same sequence twice idempotent.

@Ravi [high]: A delete event with a lower sequence number can arrive after a completion event. Review examples/task-list-student/src/sync-events.ts and keep the final state deterministic without relying on arrival order.

QA: Cover a known toggle, a missing id, duplicate events, delete-before-toggle, and out-of-order replay. Use in-memory arrays only; no browser storage or network.

Product constraint: Keep the interaction forgiving for stale clients. Do not throw from a user action merely because the record is gone.